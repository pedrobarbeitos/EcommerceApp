import React from 'react'
import Card from '../../components/Card/Card';
import "./List.scss"


function List() {

  return (
    <div className="list">
      {/*data?.map(item=>(
        <Card item={item} key={item.id}/>
      ))*/}</div>
  )
}

export default List